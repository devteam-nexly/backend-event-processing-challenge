services:
  api:
    environment:
      NODE_ENV: development
    command: npm run dev:debug
    ports:
      - "3000:3000"
      - "9229:9229"
    volumes:
      - ./apps/api:/app
      - /app/node_modules
